This folder contains data and code for generation of training data required for surrogate model.



iModel_Wgde_{value}_P_{value}_vin_{value}_dpore_{value}_dpored_{value}_Wsep_{value}_Ls_2000_datan: MATLAB Data files with AWE stack and BOP data from extracted from CFD simulations to be used by 'ANN_dataprep' for preparing the training data file 'preparedData_BoxCox_scaled.mat'

ANN_dataprep : MATLAB code for training data preparation

preparedData_BoxCox_scaled.mat : MATLAB Data file with DNN training data

